initSidebarItems({"constant":[["MAX_MATCH",""]],"fn":[["get_match_length","Get the length of the checked match The function returns number of bytes at and including `current_pos` that are the same as the ones at `pos_to_check`"],["longest_match","Try finding the position and length of the longest match in the input data. # Returns (length, distance from position) If no match is found that was better than `prev_length` or at all, or we are at the start, the length value returned will be 2."]]});